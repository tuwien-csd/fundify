import { AnnotatedCallDisplayPipe } from './annotated-call-display.pipe';

describe('AnnotatedCallDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new AnnotatedCallDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
